# RunPod ComfyUI workflows

1. Build your img2vid workflow in ComfyUI on the pod.
2. **Workflow → Export (API)** and save as `runpod_img2vid.json`.
3. Update `runpod_mapping.json` with node IDs for prompt, length, and the start image filename used in Load Image.
